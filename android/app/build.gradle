plugins {
    id("com.android.application")
    id("kotlin-android")
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id("dev.flutter.flutter-gradle-plugin")

    // Add the Google services Gradle plugin for Firebase
    id("com.google.gms.google-services")
}

android {
    namespace = "com.example.u_2marines_online"
    compileSdk = 33 // Replace this with the latest compileSdk version if necessary.
    ndkVersion = "21.1.6352462" // Use the correct NDK version or set to flutter.ndkVersion if needed

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.u_2marines_online"
        minSdk = 21 // Minimum SDK version
        targetSdk = 33 // Target SDK version
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    // Import the Firebase BoM (Bill of Materials)
    implementation(platform("com.google.firebase:firebase-bom:33.3.0"))

    // Add the Firebase Analytics SDK
    implementation("com.google.firebase:firebase-analytics")

    // Add additional Firebase SDKs as needed
    // For example:
    // implementation("com.google.firebase:firebase-auth")
    // implementation("com.google.firebase:firebase-firestore")
}

// Apply the Google services plugin for Firebase
apply(plugin = "com.google.gms.google-services")
